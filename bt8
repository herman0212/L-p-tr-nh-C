#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n;
    double fact=1.0;
    scanf("%d",&n);
    for(int i=0; i<=n; i++)
    {
        if(i>0)
        {
            fact = fact * i;
        }
    }
    printf("%d! = %.3lf\n",n,fact);
    return 0;
}
